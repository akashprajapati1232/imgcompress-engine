/**
 * @fileoverview sRGB color space utilities — V1 stub.
 *
 * Future: implement sRGB linearization and gamma correction.
 */

/** sRGB gamma encoding (linear → sRGB). Future implementation. */
export function linearToSrgb(_linear: number): number {
  // TODO: implement in future version
  return _linear;
}

/** sRGB gamma decoding (sRGB → linear). Future implementation. */
export function srgbToLinear(_srgb: number): number {
  // TODO: implement in future version
  return _srgb;
}
